
## 可能原因
- 密钥错误
- 请求参数和请求地址不是同一套环境(例如：生产环境参数用测试环境请求地址)
- 请求参数错误，使用接口不存在的字段参数
- 字段排序错误

## 解决方案
1. 确认密钥是否正确
2. 检查使用的接口请求地址
3. 检查请求的json字段和接口文档对应字段，接口文档不存在的字段则要去除
4. 按照字典序排序，支付接口使用access_token=密钥，分账和有机构参数的字段按照key=密钥拼接